{
  "version": 2,
  "buildCommand": "pnpm --filter @easyfactura/shared-types --filter @easyfactura/shared-constants --filter @easyfactura/shared-validators --filter @easyfactura/brand-config run build && prisma generate && pnpm exec nest build",
  "installCommand": "pnpm install",
  "functions": {
    "api/index.ts": {
      "includeFiles": "{dist/**,node_modules/.prisma/**,node_modules/@easyfactura/**}",
      "maxDuration": 30,
      "memory": 1024
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
