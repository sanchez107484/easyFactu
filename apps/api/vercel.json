{
  "version": 2,
  "buildCommand": "cd ../.. && pnpm --filter @easyfactura/shared-types run build && pnpm --filter @easyfactura/shared-constants run build && pnpm --filter @easyfactura/shared-validators run build && pnpm --filter @easyfactura/brand-config run build && cd apps/api && pnpm exec nest build",
  "installCommand": "pnpm install",
  "functions": {
    "api/index.ts": {
      "includeFiles": ["dist/**", "node_modules/@easyfactura/**"],
      "maxDuration": 30,
      "memory": 1024
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
